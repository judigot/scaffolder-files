# @bigbang/api

Nest.js HTTP package for the template-monorepo workspace.

```sh
bun run --filter @bigbang/api dev
```

- Health: `GET /api/health`
- Hello: `GET /api/hello`
- Schema-driven CRUD modules are generated by the project FILE_LOOP templates
