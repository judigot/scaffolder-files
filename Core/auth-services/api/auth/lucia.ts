/**
 * @deprecated Lucia is no longer the session library.
 * These aliases wrap Better Auth so older templates keep compiling.
 */
export {
  initializeBetterAuth as initializeLucia,
  getBetterAuth as getLucia,
} from './better-auth';
