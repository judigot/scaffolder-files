# PostgreSQL connection used by Drizzle (`api/db`). Required for table routes.
DATABASE_URL=postgresql://user:password@localhost:5432/app

# Listen port for `next dev` / `next start` (golden tests set this).
PORT=3000
