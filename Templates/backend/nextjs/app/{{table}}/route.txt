import type { NextApiRequest, NextApiResponse } from 'next';
import { type I{{tableName.singular.pascalCase}} } from '@/interfaces/I{{tableName.singular.pascalCase}}';

// Mock database - in a real app, this would be replaced with actual database calls
let mockDb: I{{tableName.singular.pascalCase}}[] = [];
let nextId = 1;

type ResponseData = {
  data?: I{{tableName.singular.pascalCase}} | I{{tableName.singular.pascalCase}}[];
  message?: string;
  error?: string;
};

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse<ResponseData>
) {
  try {
    const { method } = req;
    const { id } = req.query;

    switch (method) {
      case 'GET':
        // Get all {{tableName.plural.pascalCase}} or a specific one by ID
        if (id) {
          const item = mockDb.find(item => String(item.id) === id);
          if (!item) {
            return res.status(404).json({ 
              error: '{{tableName.singular.pascalCase}} not found' 
            });
          }
          return res.status(200).json({ 
            data: item,
            message: '{{tableName.singular.pascalCase}} retrieved successfully' 
          });
        } else {
          return res.status(200).json({ 
            data: mockDb,
            message: '{{tableName.plural.pascalCase}} retrieved successfully' 
          });
        }

      case 'POST':
        // Create new {{tableName.singular.pascalCase}}
        const newItem: I{{tableName.singular.pascalCase}} = {
          id: nextId++,
          ...(req.body as Omit<I{{tableName.singular.pascalCase}}, 'id'>),
          createdAt: new Date().toISOString(),
          updatedAt: new Date().toISOString()
        };
        mockDb.push(newItem);
        return res.status(201).json({ 
          data: newItem,
          message: '{{tableName.singular.pascalCase}} created successfully' 
        });

      case 'PUT':
        // Update {{tableName.singular.pascalCase}}
        if (!id) {
          return res.status(400).json({ 
            error: 'ID is required for update' 
          });
        }
        
        const existingIndex = mockDb.findIndex(item => String(item.id) === id);
        if (existingIndex === -1) {
          return res.status(404).json({ 
            error: '{{tableName.singular.pascalCase}} not found' 
          });
        }
        
        const updatedItem: I{{tableName.singular.pascalCase}} = {
          ...mockDb[existingIndex],
          ...req.body,
          updatedAt: new Date().toISOString()
        };
        
        mockDb[existingIndex] = updatedItem;
        
        return res.status(200).json({ 
          data: updatedItem,
          message: '{{tableName.singular.pascalCase}} updated successfully' 
        });

      case 'DELETE':
        // Delete {{tableName.singular.pascalCase}}
        if (!id) {
          return res.status(400).json({ 
            error: 'ID is required for deletion' 
          });
        }
        
        const initialLength = mockDb.length;
        mockDb = mockDb.filter(item => String(item.id) !== id);
        
        if (mockDb.length === initialLength) {
          return res.status(404).json({ 
            error: '{{tableName.singular.pascalCase}} not found' 
          });
        }
        
        return res.status(200).json({ 
          message: '{{tableName.singular.pascalCase}} deleted successfully' 
        });

      default:
        res.setHeader('Allow', ['GET', 'POST', 'PUT', 'DELETE']);
        return res.status(405).json({ error: `Method ${String(method)} Not Allowed` });
    }
  } catch (error) {
    console.error('API error:', error);
    return res.status(500).json({ 
      error: 'Internal Server Error',
    });
  }
} 