import React from 'react';
import { GetServerSideProps } from 'next';
import { I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}';
import { use{{tableNamePascalCaseSingular}} } from '@/hooks/{{tableNameKebabCaseSingular}}/use{{tableNamePascalCaseSingular}}';

interface I{{tableNamePascalCaseSingular}}PageProps {
  initialData?: I{{tableNamePascalCaseSingular}}[];
}

export default function {{tableNamePascalCaseSingular}}Page({ initialData }: I{{tableNamePascalCaseSingular}}PageProps) {
  const { 
    data, 
    isLoading, 
    error,
    create{{tableNamePascalCaseSingular}},
    update{{tableNamePascalCaseSingular}},
    delete{{tableNamePascalCaseSingular}} 
  } = use{{tableNamePascalCaseSingular}}(initialData);

  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error: {String(error.message)}</div>;

  return (
    <div>
      <h1>{{tableNamePascalCaseSingular}} Management</h1>
      {/* Render your data and form components here */}
    </div>
  );
}

export const getServerSideProps: GetServerSideProps = async () => {
  // Fetch initial data
  return {
    props: {
      initialData: [],
    },
  };
}; 