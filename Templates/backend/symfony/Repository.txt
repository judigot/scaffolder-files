<?php

namespace App\Repository;

use App\Entity\{{tableNamePascalCaseSingular}};
use Doctrine\Bundle\DoctrineBundle\Repository\ServiceEntityRepository;
use Doctrine\Persistence\ManagerRegistry;

/**
 * @extends ServiceEntityRepository<{{tableNamePascalCaseSingular}}>
 */
class {{tableNamePascalCaseSingular}}Repository extends ServiceEntityRepository
{
    public function __construct(ManagerRegistry $registry)
    {
        parent::__construct($registry, {{tableNamePascalCaseSingular}}::class);
    }

    public function save({{tableNamePascalCaseSingular}} $entity, bool $flush = false): void
    {
        $this->getEntityManager()->persist($entity);

        if ($flush) {
            $this->getEntityManager()->flush();
        }
    }

    public function remove({{tableNamePascalCaseSingular}} $entity, bool $flush = false): void
    {
        $this->getEntityManager()->remove($entity);

        if ($flush) {
            $this->getEntityManager()->flush();
        }
    }

    /**
     * @return {{tableNamePascalCaseSingular}}[] Returns an array of {{tableNamePascalCaseSingular}} objects
     */
    public function findByExampleField($value): array
    {
        return $this->createQueryBuilder('{{tableNameFirstLetterLowerCase}}')
            ->andWhere('{{tableNameFirstLetterLowerCase}}.exampleField = :val')
            ->setParameter('val', $value)
            ->orderBy('{{tableNameFirstLetterLowerCase}}.id', 'ASC')
            ->setMaxResults(10)
            ->getQuery()
            ->getResult()
        ;
    }
} 