import React from 'react';
import { I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}';

interface I{{tableNamePascalCaseSingular}}ComponentProps {
  data: I{{tableNamePascalCaseSingular}}[];
  onEdit: (item: I{{tableNamePascalCaseSingular}}) => void;
  onDelete: (id: number) => void;
}

export default function {{tableNamePascalCaseSingular}}Component({ 
  data, 
  onEdit, 
  onDelete 
}: I{{tableNamePascalCaseSingular}}ComponentProps) {
  return (
    <div className="container mx-auto p-4">
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {data.map((item) => (
          <div key={item.id} className="border rounded-lg p-4 shadow-sm">
            <h2 className="text-xl font-bold">{item.name}</h2>
            <div className="mt-4 flex justify-end space-x-2">
              <button 
                onClick={() => { onEdit(item); }}
                className="px-4 py-2 bg-blue-500 text-white rounded"
              >
                Edit
              </button>
              <button 
                onClick={() => { onDelete(item.id); }}
                className="px-4 py-2 bg-red-500 text-white rounded"
              >
                Delete
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
} 