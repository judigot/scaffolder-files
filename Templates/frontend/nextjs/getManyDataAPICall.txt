import { I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}';

/**
 * Fetches all {{tableNamePascalCasePlural}} from the API
 * @returns Promise resolving to an array of {{tableNamePascalCaseSingular}} objects
 */
export const get{{tableNamePascalCasePlural}} = async (): Promise<I{{tableNamePascalCaseSingular}}[]> => {
  try {
    const response = await fetch('/api/{{tableNameKebabCasePlural}}', {
      method: 'GET',
      headers: {
        Accept: 'application/json',
        'Content-Type': 'application/json',
      },
      cache: 'no-store', // Don't cache this request
    });

    if (!response.ok) {
      throw new Error(`HTTP error: ${String(response.status)}`);
    }

    const result = await response.json();
    return result.data || [];
  } catch (error: unknown) {
    if (typeof error === 'string') {
      throw new Error(`Failed to fetch {{tableNameCamelCasePlural}}: ${error}`);
    }
    if (error instanceof Error) {
      throw new Error(`Failed to fetch {{tableNameCamelCasePlural}}: ${String(error.message)}`);
    }
    throw new Error('An unknown error occurred');
  }
}; 