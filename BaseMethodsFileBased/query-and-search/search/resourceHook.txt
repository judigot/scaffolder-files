import { useQuery, type UseQueryOptions } from '@tanstack/react-query';
import { searchResources } from '@/hooks/shared/useSearch.ts';
import { type I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

interface ISearchParams {
  query: string;
  columns?: string[];
  filters?: Record<string, unknown>;
  limit?: number;
}

export const useSearch{{tableNamePascalCasePlural}} = (
  params: ISearchParams,
  options?: Omit<
    UseQueryOptions<I{{tableNamePascalCaseSingular}}[]>,
    'queryKey' | 'queryFn'
  >,
) => {
  return useQuery({
    queryKey: ['{{tableNameCamelCasePlural}}', 'search', params.query, params.columns, params.filters, params.limit],
    queryFn: () =>
      searchResources<I{{tableNamePascalCaseSingular}}>(
        '/{{tableNameCamelCasePlural}}/search',
        params.query,
        params.columns,
        params.filters,
        params.limit
      ),
    enabled: Boolean(params.query?.trim()),
    ...options,
  });
}; 