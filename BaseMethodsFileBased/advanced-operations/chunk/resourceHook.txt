import { useQuery, type UseQueryOptions } from '@tanstack/react-query';
import { chunkResources } from '@/hooks/shared/useChunk.ts';
import { type I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

interface IChunkParams {
  size: number;
  filters?: Record<string, unknown>;
}

export const useChunk{{tableNamePascalCasePlural}} = (
  params: IChunkParams,
  options?: Omit<
    UseQueryOptions<I{{tableNamePascalCaseSingular}}[][]>,
    'queryKey' | 'queryFn'
  >,
) => {
  return useQuery({
    queryKey: ['{{tableNameCamelCasePlural}}', 'chunk', params.size, params.filters],
    queryFn: () =>
      chunkResources<I{{tableNamePascalCaseSingular}}>(
        '/{{tableNameCamelCasePlural}}/chunk',
        params.size,
        params.filters
      ),
    ...options,
  });
}; 