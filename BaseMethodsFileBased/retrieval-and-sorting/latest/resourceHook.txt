import { useQuery, UseQueryOptions } from '@tanstack/react-query';
import { getLatestResources } from '@/hooks/shared/useLatest.ts';
import { I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

interface ILatestParams {
  limit?: number;
  filters?: Record<string, unknown>;
}

export const useLatest{{tableNamePascalCasePlural}} = (
  params: ILatestParams = {},
  options?: Omit<
    UseQueryOptions<I{{tableNamePascalCaseSingular}}[], Error>,
    'queryKey' | 'queryFn'
  >,
) => {
  return useQuery({
    queryKey: ['{{tableNameCamelCasePlural}}', 'latest', params.limit, params.filters],
    queryFn: () =>
      getLatestResources<I{{tableNamePascalCaseSingular}}>(
        '/{{tableNameCamelCasePlural}}/latest',
        params.limit,
        params.filters
      ),
    ...options,
  });
}; 