import { useQuery, type UseQueryOptions } from '@tanstack/react-query';
import { orderByResources } from '@/hooks/shared/useOrderBy.ts';
import { type I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

interface IOrderByParams {
  column: string;
  direction?: 'asc' | 'desc';
  limit?: number;
  filters?: Record<string, unknown>;
}

export const useOrderBy{{tableNamePascalCasePlural}} = (
  params: IOrderByParams,
  options?: Omit<
    UseQueryOptions<I{{tableNamePascalCaseSingular}}[]>,
    'queryKey' | 'queryFn'
  >,
) => {
  return useQuery({
    queryKey: ['{{tableNameCamelCasePlural}}', 'orderBy', params.column, params.direction, params.limit, params.filters],
    queryFn: () =>
      orderByResources<I{{tableNamePascalCaseSingular}}>(
        '/{{tableNameCamelCasePlural}}/order-by',
        params.column,
        params.direction,
        params.limit,
        params.filters
      ),
    enabled: Boolean(params.column),
    ...options,
  });
}; 