import { useMutation, useQueryClient, type UseMutationOptions } from '@tanstack/react-query';
import { {{methodNameCamelCase}} } from '@/hooks/shared/use{{methodNameCamelCase}}.ts';
import { type I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

interface IBatchCreateData {
  dataArray: Omit<I{{tableNamePascalCaseSingular}}, 'id'>[];
  options?: {
    batchSize?: number;
    rollbackOnError?: boolean;
    continueOnError?: boolean;
  };
}

export const use{{methodNameCamelCase}}{{tableNamePascalCaseSingular}} = (
  options?: Omit<
    UseMutationOptions<{
      success: I{{tableNamePascalCaseSingular}}[];
      failed: { data: Omit<I{{tableNamePascalCaseSingular}}, 'id'>; error: string; index: number }[];
      transactionId?: string;
    }, Error, IBatchCreateData>,
    'mutationFn'
  >,
) => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ dataArray, options: batchOptions }: IBatchCreateData) =>
      {{methodNameCamelCase}}<I{{tableNamePascalCaseSingular}}, Omit<I{{tableNamePascalCaseSingular}}, 'id'>>(
        '/{{tableNameCamelCasePlural}}',
        dataArray,
        batchOptions
      ),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['{{tableNameCamelCasePlural}}'] });
    },
    ...options,
  });
}; 