import { useQuery, UseQueryOptions } from '@tanstack/react-query';
import { getWithTrashedResources } from '@/hooks/shared/useWithTrashed.ts';
import { I{{tableNamePascalCaseSingular}} } from '@/interfaces/I{{tableNamePascalCaseSingular}}.ts';

interface IWithTrashedParams {
  filters?: Record<string, unknown>;
  limit?: number;
  orderBy?: string;
  orderDirection?: 'asc' | 'desc';
}

export const useWithTrashed{{tableNamePascalCasePlural}} = (
  params: IWithTrashedParams = {},
  options?: Omit<
    UseQueryOptions<I{{tableNamePascalCaseSingular}}[], Error>,
    'queryKey' | 'queryFn'
  >,
) => {
  return useQuery({
    queryKey: ['{{tableNameCamelCasePlural}}', 'withTrashed', params.filters, params.limit, params.orderBy, params.orderDirection],
    queryFn: () =>
      getWithTrashedResources<I{{tableNamePascalCaseSingular}}>(
        '/{{tableNameCamelCasePlural}}/with-trashed',
        params.filters,
        params.limit,
        params.orderBy,
        params.orderDirection
      ),
    ...options,
  });
}; 