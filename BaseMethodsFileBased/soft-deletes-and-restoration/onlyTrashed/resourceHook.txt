import { useQuery, type UseQueryOptions } from '@tanstack/react-query';
import { getOnlyTrashedResources } from '@/hooks/shared/useOnlyTrashed.ts';
import { type I{{tableName.singular.pascalCase}} } from '@/interfaces/I{{tableName.singular.pascalCase}}.ts';

interface IOnlyTrashedParams {
  filters?: Record<string, unknown>;
  limit?: number;
  orderBy?: string;
  orderDirection?: 'asc' | 'desc';
}

export const useOnlyTrashed{{tableName.plural.pascalCase}} = (
  params: IOnlyTrashedParams = {},
  options?: Omit<
    UseQueryOptions<I{{tableName.singular.pascalCase}}[]>,
    'queryKey' | 'queryFn'
  >,
) => {
  return useQuery({
    queryKey: ['{{tableName.plural.camelCase}}', 'onlyTrashed', params.filters, params.limit, params.orderBy, params.orderDirection],
    queryFn: () =>
      getOnlyTrashedResources<I{{tableName.singular.pascalCase}}>(
        '/{{tableName.plural.camelCase}}/only-trashed',
        params.filters,
        params.limit,
        params.orderBy,
        params.orderDirection
      ),
    ...options,
  });
}; 