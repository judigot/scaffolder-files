<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Employee ID - [[USE_DATA(basic-info.name)]]</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background: #f5f5f5; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
    .id-card { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; width: 400px; height: 250px; border-radius: 15px; padding: 25px; box-shadow: 0 10px 30px rgba(0,0,0,0.3); position: relative; overflow: hidden; }
    .id-card::before { content: ''; position: absolute; top: -50%; right: -50%; width: 200%; height: 200%; background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%); }
    .card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; position: relative; z-index: 1; }
    .company-name { font-size: 1.2em; font-weight: bold; }
    .employee-photo { width: 80px; height: 80px; border-radius: 50%; border: 3px solid white; background: #ddd; display: flex; align-items: center; justify-content: center; font-size: 2em; position: relative; z-index: 1; }
    .card-body { display: flex; gap: 20px; position: relative; z-index: 1; }
    .employee-info { flex: 1; }
    .employee-name { font-size: 1.5em; font-weight: bold; margin-bottom: 10px; }
    .employee-details { font-size: 0.9em; opacity: 0.9; line-height: 1.6; }
    .employee-id { font-size: 1.1em; font-weight: bold; margin-top: 15px; padding-top: 15px; border-top: 2px solid rgba(255,255,255,0.3); }
    .barcode { margin-top: 15px; text-align: center; font-family: monospace; font-size: 1.2em; letter-spacing: 3px; position: relative; z-index: 1; }
  </style>
</head>
<body>
  <div class="id-card">
    <div class="card-header">
      <div class="company-name">[[USE_DATA(company-info.name)]]</div>
      <div class="employee-photo">[[USE_DATA(basic-info.name[0])]]</div>
    </div>
    <div class="card-body">
      <div class="employee-info">
        <div class="employee-name">[[USE_DATA(basic-info.name)]]</div>
        <div class="employee-details">
          <div><strong>ID:</strong> [[USE_DATA(employee.employee-id)]]</div>
          <div><strong>Dept:</strong> [[USE_DATA(employee.department)]]</div>
          <div><strong>Position:</strong> [[USE_DATA(employee.position)]]</div>
          <div><strong>Status:</strong> [[USE_DATA(employee.employee-status)]]</div>
        </div>
        <div class="employee-id">Employee ID: [[USE_DATA(employee.employee-id)]]</div>
      </div>
    </div>
    <div class="barcode">|||| |||| |||| ||||</div>
  </div>
</body>
</html>

